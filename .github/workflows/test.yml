name: Simple Test

on:
  pull_request:
    branches:
      - main
    types: [closed]

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: echo "Hello from GitHub Actions!"